[pytest]
testpaths = tests
addopts = -q --strict-markers
asyncio_mode = strict
markers =
    slow: exercises a real browser or takes noticeably longer than the unit suite
    browser: drives Playwright against a live or file:// page
